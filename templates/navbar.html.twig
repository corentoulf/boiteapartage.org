<nav class="navbar navbar-expand-lg justify-content-between py-1 bg-primary">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{path('app_home')}}">
            <img class="d-none" src="{{asset('img/logo-bleu-petard.svg')}}" alt="La Boîte à Partage" width="35" height="35">
            <span class="navbar-brand fs-4 ff-serif ms-0 ms-2 text-bg-primary">boîte à partage<span class="ms-2 text-highlight">●</span></span>
        </a>
        <ul class="d-flex flex-row align-items-center gap-2 navbar-nav ms-auto align-items-start align-items-lg-center">
            {% if app.user %}
                <li class="nav-item d-flex ms-2">
                    <a class="btn btn-sm btn-outline-light" href="{{ path('app_auth_home') }}">Aller sur l'App<i class="bi bi-arrow-right ms-2"></i></a>
                </li>
            {% else %}
                <li class="nav-item d-flex">
                    <a class="btn btn-sm btn-outline-secondary px-3" href="{{ path('app_login') }}"></i><span class="">Se connecter</span></a>
                </li>
                <li class="nav-item d-none d-sm-flex">
                    <a class="btn btn-sm btn-highlight px-3" href="{{path('app_register', {'registrationPurpose': 'createCircle'})}}"><span class="">Créer une boîte</span><i class="bi bi-arrow-right ms-2"></i></a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
{# <nav class="navbar justify-content-between border-bottom d-flex d-lg-none">
    {% if app.user %}
        <div class="container d-flex justify-content-between justify-content-sm-end">
            <form class="" role="search" aria-label="search" id="homeSearchForm" method="POST" action="{{path('app_items_search')}}">
                <div class="input-group input-group-sm">
                    <input class="form-control" type="text" style="" placeholder="Rechercher dans les boîtes" aria-label="input search" id="searchTermsInput" name="searchTerms">
                    <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i></button>
                </div>
            </form>
            <a class="btn btn-sm btn-outline-primary ms-sm-2" href="{{ path('app_auth_home') }}"><i class="bi bi-box-arrow-up-right me-2 "></i>Aller sur l'App</a>
        </div>
    {% else %}
        <div class="container d-flex justify-content-end">
            <a class="btn btn-sm btn-primary" href="{{path('app_register', {'registrationPurpose': 'createCircle'})}} }}"><i class="bi bi-rocket-takeoff me-2"></i><span class="">Créer une boîte</span></a>
            <a class="btn btn-sm btn-outline-primary border-0 ms-2" href="{{ path('app_login') }}"><i class="bi bi-person-circle me-2"></i><span class="">Connexion | Inscription</span></a>
            <a class="d-none nav-link link-dark link-underline-0 fs-4 ms-2" href="{{ path('app_auth_home') }}"><i class="bi bi-question-circle"></i></a>
        </div>
    {% endif %}
</nav> #}