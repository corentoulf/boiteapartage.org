<nav class="navbar navbar-expand-lg justify-content-between border-bottom">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center ms-2 ms-lg-0" href="{{path('app_home')}}">
            <img class="" src="{{asset('img/logo-bleu-petard.svg')}}" alt="La Boîte à Partage" width="35" height="35">
            <span class="navbar-brand fw-bold ms-0 ms-2">Boîte à Partage</span>
        </a>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler" aria-controls="#navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarToggler">
            <ul class="navbar-nav ms-auto  align-items-start align-items-lg-center">
             {# mb-2 mb-lg-0 #}
            {# <div class="d-flex justify-content-between align-items-center px-5 py-3">
                <div class="pe-5">
                    <div class="collapse navbar-collapse" id="navbarNavDropdown">
                        <ul class="navbar-nav">
                            <li class="nav-item me-6">
                                <a class="nav-link link-dark {% if app.current_route|split('_')[1] == 'register' and registrationPurpose == 'createCircle' %}fw-bold{% endif %}" href="{{path('app_register', {'registrationPurpose': 'createCircle'})}}"><i class="bi bi-plus-square me-2"></i>Créer une boîte</a>
                            </li>
                            <li class="nav-item me-6">
                                <a class="nav-link link-dark {% if app.current_route|split('_')[1] == 'register' and registrationPurpose in ['joinCircle','joinCircleId'] %}fw-bold{% endif %}" href="{{path('app_register', {'registrationPurpose': 'joinCircle'})}}"><i class="bi bi-box-arrow-in-up-right me-2"></i>Rejoindre une boîte</a>
                            </li>
                            <li class="nav-item me-6">
                                <a class="nav-link link-dark {% if app.current_route == 'app_manifeste' %}fw-bold{% endif %}" href="{{ path('app_manifeste') }}"><i class="bi bi-file-text me-2"></i>Lire <span class="bg-info p-1 text-light">le Manifeste</span></a>
                            </li>
                        </ul>
                    </div>
                </div>
                <button class="navbar-toggler ms-3" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div> #}
                {% if app.user %}
                    <form class="ms-lg-auto d-none d-lg-flex" role="search" aria-label="search" id="homeSearchForm" method="POST" action="{{path('app_circle_search')}}">
                        <div class="input-group input-group-sm">
                            <input class="form-control" type="text" style="min-width: 300px;" placeholder="Rechercher dans les boîtes" aria-label="input search" id="searchTermsInput" name="searchTerms">
                            <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i></button>
                        </div>
                    </form>
                    <li class="nav-item d-none d-lg-flex ms-2">
                        <a class="btn btn-sm btn-primary" href="{{ path('app_auth_home') }}"><i class="bi bi-box-arrow-in-right me-2 "></i>Aller sur l'App</a>
                    </li>
                {% else %}
                    <li class="nav-item d-none d-lg-flex">
                        <a class="btn btn-sm btn-primary" href="{{path('app_register', {'registrationPurpose': 'createCircle'})}} }}"><i class="bi bi-rocket-takeoff me-2"></i><span class="">Créer une boîte</span></a>
                    </li>
                    <li class="nav-item ms-2 d-none d-lg-flex">
                        <a class="btn btn-sm btn-outline-primary border-0" href="{{ path('app_login') }}"><i class="bi bi-person-circle me-2"></i><span class="">Connexion | Inscription</span></a>
                    </li>
                {% endif %}
                <li class="nav-item d-none d-lg-flex ms-2">
                    <a class="nav-link link-dark link-underline-0 fs-4" href="{{ path('app_auth_home') }}"><i class="bi bi-question-circle"></i></a>
                </li>
                <li class="nav-item d-flex d-lg-none align-items-center ms-2">
                    <a class="nav-link link-dark link-underline-0 fs-4" href="{{ path('app_auth_home') }}"><i class="bi bi-question-circle me-2"></i></a>Comment ça marche
                </li>
            </ul>
        </div>
    </div>
</nav>
<nav class="navbar justify-content-between border-bottom d-flex d-lg-none">
    <div class="container d-flex justify-content-end">
        {% if app.user %}
            <form class="" role="search" aria-label="search" id="homeSearchForm" method="POST" action="{{path('app_circle_search')}}">
                <div class="input-group input-group-sm">
                    <input class="form-control" type="text" style="min-width: 300px;" placeholder="Rechercher dans les boîtes" aria-label="input search" id="searchTermsInput" name="searchTerms">
                    <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i></button>
                </div>
            </form>
            <a class="btn btn-sm btn-primary" href="{{ path('app_auth_home') }}"><i class="bi bi-box-arrow-in-right me-2 "></i>Aller sur l'App</a>
        {% else %}
            <a class="btn btn-sm btn-primary" href="{{path('app_register', {'registrationPurpose': 'createCircle'})}} }}"><i class="bi bi-rocket-takeoff me-2"></i><span class="">Créer une boîte</span></a>
            <a class="btn btn-sm btn-outline-primary border-0 ms-2" href="{{ path('app_login') }}"><i class="bi bi-person-circle me-2"></i><span class="">Connexion | Inscription</span></a>
            <a class="d-none nav-link link-dark link-underline-0 fs-4 ms-2" href="{{ path('app_auth_home') }}"><i class="bi bi-question-circle"></i></a>

        {% endif %}
    </div>
</nav>