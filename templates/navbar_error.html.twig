<nav class="navbar navbar-expand-lg py-4 justify-content-center">
  <div class="container d-flex justify-content-between align-items-center px-5 py-3 border shadow-lg border-2 border-info">
        <a class="navbar-brand d-flex align-items-center me-6" href="{{path('app_home')}}">
            <img src="{{asset('img/logo-pizza-square.svg')}}" alt="La Boîte à Partage" width="50" height="50">
            <span class="navbar-brand fs-2 fw-bold ms-3">Boîte à Partage</span>
        </a>
        {% if app.user %}
            <a class="btn btn-outline-primary" href="{{ path('app_auth_home') }}"><i class="bi bi-box-arrow-up-right me-1"></i>Aller sur l'App</a>
        {% else %}
            <a class="btn btn-outline-primary" href="{{ path('app_login') }}"><i class="bi bi-person-circle me-1"></i>Connexion</a>
        {% endif %}
        
  </div>
</nav>