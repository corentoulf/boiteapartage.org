<nav class="navbar navbar-expand-lg bg-body-tertiary py-4">
  <div class="container d-flex justify-content-between">
        <a class="navbar-brand fs-2 px-4 fw-bold" href="{{path('app_home')}}">La Boîte à Partage</a>
        <div class="px-4">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item me-5">
                        <a class="nav-link" href="{{ path('app_auth_home') }}"><i class="bi bi-house me-1"></i>Accueil</a>
                    </li>
                    <li class="nav-item me-5">
                        <a class="nav-link" href="{{ path('app_item') }}"><i class="bi bi-card-list me-1"></i>Mon placard</a>
                    </li>
                    <li class="nav-item dropdown me-5">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-arrow-repeat me-1"></i>Cercles
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{path("app_circle")}}"><i class="bi bi-people me-1"></i>Mes cercles</a></li>
                            <li><a class="dropdown-item" href="{{path("app_circle_create")}}"><i class="bi bi-plus-circle me-1"></i>Créer un cercle</a></li>
                            <li><a class="dropdown-item" href="{{path("app_circle_join")}}"><i class="bi bi-person-fill-add me-1"></i>Rejoindre un cercle</a></li>
                        </ul>
                    </li>
                    <li class="nav-item me-5">
                        <a class="nav-link" href="{{ path('app_user_profile') }}"><i class="bi bi-person-circle me-1"></i>Compte</a>
                    </li>
                    {% if is_granted("ROLE_ADMIN") %}
                        <li class="nav-item me-5">
                            <a class="nav-link" href="{{ path('app_admin') }}"><i class="bi bi-gear-fill me-1"></i>Admin</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
        <a class="btn btn-outline-danger  px-4" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-1"></i>Déconnexion</a>
  </div>
</nav>