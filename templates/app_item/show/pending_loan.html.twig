{% extends 'base_auth.html.twig' %}

{% block title %}Mon Placard{% endblock %}
{% set defaultImgUrl= asset('img/no-cover-picture-alt.svg') %}
{% set allowClickOnItem = false %}
{% set cardHighlightedWithShadow = true %}
{% block body %}
<div class="container min-vh-100">
    <div class="col-md-8 offset-md-2 px-2 py-3 p-lg-5">
      {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
      {% endfor %}
      <div class="align-items-center d-flex flex-row justify-content-between">
        <h1 class="fw-bold fs-3" id="title">Emprunter <span class="text-highlight">{{item.property1}}</span></h1>
      </div>
      <div class="row py-4">
        <div class="col">
          {{ include('_component/item_card.html.twig') }}
        </div>
      </div>
      <div class="alert alert-success alert-static mt-3" role="alert">
        <h4 class="alert-heading">Demande envoyée !</h4>
        <p>Vous avez effectué une demande d'emprunt le <b>{{loans[0].createdAt|date("d/m/Y")}}</b>.</p>

        <div class="d-flex flex-row justify-content-end">
          <a
            type="button"
            name=""
            id=""
            href="{{path('app_show_incoming_loan', {id: loans[0].id})}}"
            class="btn btn-outline-dark"
          >
            Suivre ma demande <i class="bi bi-arrow-right fw-bold"></i>
          </a>
        </div>
      </div>
    </div>
</div>

{% endblock %}
{% block javascripts %}
    {% block importmap %}
     {{ importmap(['app', 'itemIndex']) }}
    {% endblock %}
{% endblock %}