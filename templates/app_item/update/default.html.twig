{% extends 'base_auth.html.twig' %}

{% block title %}Ajout au Placard{% endblock %}

{% block body %}

<div class="container">
    <div class="col-md-8 offset-md-2 px-2 py-3 p-lg-5">
        {% for flash_error in app.flashes('verify_email_error') %}
            <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %}
        <div class="align-items-center d-flex flex-row justify-content-between mb-3">
            <h1 class="fw-bold fs-3" id="title">
                Mettre à jour l'objet
            </h1>
        </div>
        {# message d'alerte sur le côté global d'un objet dispo dans tous les cercles #}
        <div class="alert alert-warning  d-none" role="alert">
            <div class="d-flex justify-content-start">
                <span class="me-3"><i class="bi bi-info-circle"></i></span>
                <span>Veuillez noter que par défaut, si vous avez rejoint plusieurs boîtes, <strong>tous les éléments</strong> de votre placard seront partagés dans <strong>toutes vos boîtes</strong>.</span>
            </div>
        </div>
        {{ form_errors(form) }}
        {{ form_start(form) }}
            {{ form_row(form.itemType)}}
            {{ form_row(form.property_1)}}
            {{ form_row(form.property_2)}}
            {{ form_row(form.imageFile)}}
                <div id="imageCoverContainer" class="">
                    {% if itemCategory.code == "bibliotheque" and not item.property3 %}
                        <div id="photoCaptureTip" class="form-text mb-2 help-text">Conseil : posez le livre sur une table pour prendre la couverture dans son intégralité.</div>
                    {% endif %}
                    <a 
                        href="#" 
                        id="openFileInputBtn" 
                        class="btn btn-dark" 
                        {% if item.imageName %}
                            style="display:none;"
                        {% endif %}
                    >
                        <i class="bi bi-camera-fill me-2"></i>Ajouter une photo
                    </a>
                    <div class="d-flex flex-column align-items-start mb-3">
                        <div class="col-8 col-md-4">
                            <img 
                                id="imgBookCover" 
                                class="img-fluid" 
                                {% if not item.imageName %}
                                    style="display:none;"
                                {% else %}
                                    src="{{vich_uploader_asset(item, 'imageFile')}}"     
                                {% endif %}
                            />
                        </div>
                        <div 
                            id="retakePhotoBtn"
                            class="mt-2"
                            {% if not item.imageName %}
                                style="display:none;"
                            {% endif %}
                        >
                            <a href="#" class="link-dark link-offset-2 link-opacity-75 link-underline-opacity-25 link-underline-opacity-75-hover"><i class="me-2 bi bi-arrow-counterclockwise"></i>Remplacer la photo</a>
                        </div>
                    </div>
                </div>
            <div class="row ">
                <div class="col d-flex justify-content-end">
                    {{ form_row(form.submitAndAdd) }}
                    <div class="ms-2">
                        {{ form_row(form.submit) }}
                    </div>
                </div>
            </div>
        {{ form_end(form) }}
   
    </div>
</div>


{% endblock %}
{% block javascripts %}
    {% block importmap %}
     {{ importmap(['app', 'itemAddDefault']) }}
    {% endblock %}
{% endblock %}