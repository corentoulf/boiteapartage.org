{% extends 'base_auth.html.twig' %}

{% block title %}Favoris • Boîte à Partage{% endblock %}

{% block body %}
<div class="container min-vh-100">
    <div class="px-lg-6">
      <div class="container-fluid" >
        {% if items|length > 0 %}
            <div class="d-flex flex-row justify-content-center mt-4 px-lg-5" style="">
                <div class="card card-bg-img-highlight col-12 m-sm-4 p-2">
                <div class="card-body text-center px-4">
                    <div class="card-title fs-5 fw-bold " >
                        Ça y est, on se décide à emprunter son petit favori ?
                    </div>
                    <a class="btn btn-primary mt-2 mt-md-4" href="#"><i class="bi bi-journal-text me-2"></i>Consulter notre guide</span></a>
                </div>
                </div>
            </div>
        </div>
        {% endif %}
        {% if items|length == 0 %}
            <div class="mt-4 text-center">
                <div class="py-3">
                    <p class="fs-4" >Vous n'avez pas encore ajouté d'objet en favoris.</p>
                    <p class="fw-bold fs-4" >On explore les catégories ?</p>
                </div>
                <div class="mt-4">
                    {% include '_component/category_explorer.html.twig' %}
                </div>
            </div>
        {% endif %}
        {% if items|length > 0 %}
            <div class="row my-4 px-lg-6">
                <div class="input-group col-md-8">
                    <input id="filterItemsInput" class="form-control" type="text" placeholder="Filtrer les favoris" aria-label="input search" name="searchTerms">
                    <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i></button>
                </div>
                <div class="col-md-4 mt-2 mt-md-0">
                    <select class="form-select" id="filterItemsCategorySelect">
                        <option selected  value="">Filtrer par catégorie</option>
                        {% for itemCategory in itemCategories %}
                            <option value="{{itemCategory.code}}">{{itemCategory.label}}</option>
                        {% endfor %}
                    </select>
                </div>
                <input id="filterItemsGlobalInput" hidden type="text" name="searchTerms">
            </div>
            <div class="row my-4 px-lg-6">
                <div id="items">
                    <span class="opacity-75"><span id="itemFoundCount">{{items|length}}</span> objet(s)</span>
                    <ul class="list-group list-group-flush mt-3 list">
                        {% set defaultImgUrl= asset('img/no-cover-picture-alt.svg') %}
                        {% for item in items %} 
                            {{ include('_component/favorite_item_list_card.html.twig') }}
                        {% endfor %}
                    </ul>
                    <ul class="pagination"></ul>
                </div>
            </div>
        {% endif %}
      </div>
    </div>
</div>
{% endblock %}
{% block javascripts %}
    {% block importmap %}
     {{ importmap(['app', 'itemSearch']) }}
    {% endblock %}
{% endblock %}