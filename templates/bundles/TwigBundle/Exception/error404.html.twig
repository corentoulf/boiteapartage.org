{% extends 'base_error.html.twig' %}

{% block title %}Alors là, on a pas trouvé !{% endblock %}
{% block body %}
<div class="container vh-100">
    <div class="p-5 my-4  ">
      <div class="container-fluid py-5">
        <div class="row align-items-center">
          <div class="col-md-4 text-center">
            <i class="bi bi-binoculars" style="font-size:8rem;"></i>
          </div>
          <div class="col-md-8">
            <h1 class="display-5 fw-bold">On a beau chercher...</h1>
            <p class="fs-4">{{exception.message == '' ? status_text : exception.message}}</p>
            <p class="fs-4">Veuillez retourner sur la page d'accueil et réessayer.</p>
            <a href="{{path('app_home')}}" class="btn btn-primary btn-lg" type="button"><i class="bi bi-arrow-left me-2"></i>Retourner à l'accueil</a>
          </div>
        </div>
        
      </div>
    </div>
</div>
{% endblock %}